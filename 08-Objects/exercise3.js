//
const signup = (email, password, username) => {
    statement = `${email} ${password} ${username}`
    return statement;
} 
console.log(signup("em@gmail.com", 2222, "emrysz"));

if (statement === )